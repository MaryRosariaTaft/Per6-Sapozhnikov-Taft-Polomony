void setup(){
  //testing
  size(500,500);
  Square s = new Square(0,0,50,50);
  s.draw();
  
  //init LL of Squares, print each around border
  //init 2 LLs of Cards, print rectangles with access to top cards
  //init 2 Dice (Dies?)
  //set up players in LL (somehow)
}
